{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn hospital_management.wsgi:application --bind 0.0.0.0:${PORT:-8000}"
  }
}
